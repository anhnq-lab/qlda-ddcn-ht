// Re-export from employees feature so other features (e.g. annual-plan)
// can import from components/common instead of cross-importing from features/employees.
export { default } from '../../features/employees/EmployeeSlideContent';
